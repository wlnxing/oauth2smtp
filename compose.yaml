services:
  oauth2smtp:
    build:
      context: .
      dockerfile: Dockerfile
    image: oauth2smtp:local
    container_name: oauth2smtp
    restart: always
    ports:
      - "2525:2525"
    volumes:
      - ./config.yaml:/config/config.yaml:rw
